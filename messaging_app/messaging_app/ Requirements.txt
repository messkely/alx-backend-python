# Generate this file using: pip freeze > requirements.txt
# Common Django + MySQL dependencies (example):

Django==4.2.7
mysqlclient==2.2.0
python-decouple==3.8
django-cors-headers==4.3.1
djangorestframework==3.14.0
gunicorn==21.2.0

# Add your actual project dependencies here after running:
# pip freeze > requirements.txt